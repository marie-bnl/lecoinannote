{
  "name": "lecoinannote",
  "version": "1.0",
  "scripts": {
    "dev": "(web-ext run -u $(cat testurl.txt) &) && rspack build --watch",
    "build": "rspack build && web-ext build",
    "clean": "rm -r dist web-ext-artifacts"
  },
  "devDependencies": {
    "@rspack/cli": "^1.4.10",
    "@rspack/core": "^1.4.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "web-ext": "^8.9.0"
  }
}
