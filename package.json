{
  "name": "lecoinannote",
  "version": "1.2",
  "scripts": {
    "dev": "(web-ext -s dist run -u $(cat testurl.txt) &) && rspack build --watch",
    "build": "rspack build && web-ext -s dist build",
    "lint": "rspack build && web-ext -s dist lint",
    "clean": "rm -r dist web-ext-artifacts"
  },
  "devDependencies": {
    "@rspack/cli": "^1.4.10",
    "@rspack/core": "^1.4.10",
    "@types/less": "^3.0.8",
    "less": "^4.4.1",
    "less-loader": "^12.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "web-ext": "^8.9.0"
  }
}
